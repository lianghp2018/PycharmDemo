1.创建集合
    使用{}或set(),空集合只能用set(),因为{}创建空字典
    *注:集合内数据不重复,无序,没有下标操作

2.集合常用操作
    集合是可变数据类型
    --增加数据
        add() 追加的是单一数据
            集合有去重功能,当追加一个集合已有的数据时将不进行任何操作

        update() 追加的是数据序列(list,tuple,dict)

    --删除数据
        remove() 删除集合内指定数据,如果数据不存在则报错

        discard() 删除集合内指定数据,数据不存在也不会报错

        pop() 随机删除集合中某个数据,并返回这个数据 (删除控制台输出最前面的数据??)

    --查找(判断)数据
        in
        not in

